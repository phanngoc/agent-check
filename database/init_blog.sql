-- MySQL initialization script for blog_demo database
-- Create database if not exists
CREATE DATABASE IF NOT EXISTS blog_demo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Use the database
USE blog_demo;

-- Grant privileges (if needed for separate user)
-- GRANT ALL PRIVILEGES ON blog_demo.* TO 'root'@'%';
-- FLUSH PRIVILEGES;

